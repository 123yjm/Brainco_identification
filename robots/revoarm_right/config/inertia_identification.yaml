# 惯性参数辨识配置 — revoarm_right
# robot / kinematic_params / data_file / output_file 由入口函数自动推导。
#
# 算法编号 (algorithm):
#   1 = OLS     4 = TLS     8 = NLS_FRICTION
#   2 = WLS     5 = EKF     9 = PCTIKHONOV (PC辨识，配置在 inertia_PC_identification.yaml)
#   3 = IRLS    0 = 全部 (benchmark, 不含 PCTIKHONOV)
#
# regularization: 1=开启零中心 Tikhonov 正则化 (λ=1e-6), 0=关闭

algorithm: 1
regularization: 1
damping: true
