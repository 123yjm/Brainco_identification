# Butterworth Filter Configuration — revoarm_right
# I/O 路径由入口函数根据 --robot 目录自动推导，无需在此配置。

filter:
  # true = 正常滤波, false = 直通（跳过 filtfilt）
  turn_on_filter: true

  sampling_frequency_hz: 100
  passband_hz: 5
  stopband_hz: 10
  passband_ripple_db: 3
  stopband_attenuation_db: 30
